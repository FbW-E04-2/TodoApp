Steps to deployment

DEPLOYMENT ----creating build folder------ 
1-add "homepage":"." in your package.json (if you use another url=> write the url instead) 
2-change BrowserRouter with HashRouter for client-side-routing (dont forget to import!) 
3- add remote origin
4- install gh-pages
5- add 2 scripts into ypour package.json
 - "predeploy" : "npm run build"
 - "deploy": "gh-pages -d build"

6- npm run deploy 